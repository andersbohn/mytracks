# Task List: User Entity + Multi-Tenancy + Track Attributes
**Timestamp:** 2026-05-22_11-23 | **Git Tag:** 77b7c74

## Migration

- [x] Create `V2__add_users_and_update_tracks.sql`
  - [x] Create `users` table: `id UUID PK`, `email VARCHAR NOT NULL UNIQUE`, `display_name VARCHAR`, `sso_provider VARCHAR NOT NULL`, `sso_subject VARCHAR NOT NULL`, `created_at TIMESTAMPTZ NOT NULL`
  - [x] Drop `tracks.user_id` (VARCHAR) + its index
  - [x] Add `tracks.user_id UUID NOT NULL REFERENCES users(id)`
  - [x] Add `tracks.activity_type VARCHAR(255)`
  - [x] Add `tracks.notes TEXT`
  - [x] Add index on `tracks(user_id)`

## Domain Model

- [x] Create `User.java` entity
  - [x] UUID id (`@GeneratedValue(strategy = GenerationType.UUID)`)
  - [x] `email` VARCHAR NOT NULL
  - [x] `displayName` VARCHAR nullable
  - [x] `ssoProvider` VARCHAR NOT NULL
  - [x] `ssoSubject` VARCHAR NOT NULL (opaque ID from Google)
  - [x] `createdAt` Instant NOT NULL
  - [x] Constructor-based injection, no-arg protected constructor
  - [x] Getters only (no setters for immutable fields)
- [x] Create `UserRepository.java` (extends `JpaRepository<User, UUID>`)
  - [x] Add `findByEmail(String email)` query method
  - [x] Add `findBySsoProviderAndSsoSubject(String provider, String subject)` query method
- [x] Update `Track.java`
  - [x] Replace `String userId` field with `@ManyToOne(optional = false) @JoinColumn(name = "user_id") User user`
  - [x] Add `activityType` VARCHAR nullable field
  - [x] Add `notes` TEXT nullable field
  - [x] Update constructor signature accordingly
  - [x] Update/add getters

## Tests

- [x] Update `MytracksApplicationTests` to verify schema loads cleanly with V2 migration
- [x] Add `UserRepositoryIT` — persist a User, find by email, find by SSO subject
- [x] Add `TrackRepositoryIT` — persist Track with User FK, assert activityType and notes round-trip
- [x] Verify `activityType` and `notes` round-trip correctly

## Definition of Done

- [x] `./mvnw spotless:check` green
- [x] `./mvnw test` green
- [x] `./mvnw verify` green (includes integration tests)
