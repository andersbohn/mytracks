# Task List — 2026-06-03_08-55 — v0.0.24

> Fit upload: (1) keep PUT /{activityId}, validate parsed id matches path param only if fit file contains an id (no filename fallback); (2) new POST endpoint — no id on path, extract from fit file or filename.

## Parser

- [x] Add `FileIdMesgListener` to `FitParser` to capture `FileIdMesg.getNumber()` (`Integer`)
- [x] Add `activityId` (`String`) field to `FitMetadata`
- [x] Populate it from file id number (Integer → unsigned String), null if absent
- [x] Update null-result `build()` path to include null activityId

## PUT /{activityId} — add validation

- [x] After parsing, if `meta.activityId()` is non-null and doesn't equal `activityId` path param, return 409
- [x] If `meta.activityId()` is null, skip validation — use path param as-is (filename never consulted)

## POST /api/tracks/fit — new endpoint

- [x] Add `POST /` handler in `FitUploadController` (no path param)
- [x] Derive id: `meta.activityId()` → strip `.fit` from `file.getOriginalFilename()` → 400 if neither
- [x] Reuse same upsert/persist logic as PUT handler (extract to shared private method)

## Frontend

- [x] Remove manual `activityId` input from `FitUpload`
- [x] Change fetch to `POST /api/tracks/fit` (no id in URL)
- [x] File selection alone enables submit

## Tests

- [x] `FitParserTest`: FIT with `fileId.setNumber(42)` → `meta.activityId()` == `"42"`
- [x] `FitParserTest`: FIT without number → `meta.activityId()` is null
- [x] `FitUploadControllerIT` — PUT: mismatched path id vs embedded id → 409
- [x] `FitUploadControllerIT` — PUT: no embedded id in FIT → succeeds (uses path param)
- [x] `FitUploadControllerIT` — POST: FIT with embedded number `98765`, filename ignored → `sourceId` == `"98765"`
- [x] `FitUploadControllerIT` — POST: FIT without embedded number, filename `"17305.fit"` → `sourceId` == `"17305"`
- [x] `FitUploadControllerIT` — POST: FIT without number + no filename → 400
- [x] `FitUploadControllerIT` — POST: guest user → 403
- [x] `FitUploadControllerIT` — POST: empty file → 400
- [x] `FitUploadControllerIT` — POST: upsert on re-upload same id

## Definition of Done

- [x] `./mvnw spotless:check` green
- [x] `./mvnw test` green (FitParserTest 4/4; MytracksApplicationTests blocked by OrbStack not running)
- [ ] Integration tests (`FitUploadControllerIT`) — blocked: OrbStack not running, start it and run `./mvnw verify`
