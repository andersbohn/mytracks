# Task Plan — 2026-05-28_16-10 — v0.0.13-3-g33ca796

> Request: Add user role enum (GUEST/USER/ADMIN). New users start as GUEST. Guests cannot upload GPX. Only admins can promote other users. Admin-only endpoint to change role.

## Backend

- [x] `V4__add_role_to_users.sql` — `ALTER TABLE users ADD COLUMN role VARCHAR(20) NOT NULL DEFAULT 'GUEST'`
- [x] `UserRole.java` — enum `GUEST, USER, ADMIN`
- [x] `User.java` — add `@Enumerated(EnumType.STRING) @Column(nullable=false) private UserRole role = UserRole.GUEST;` + getter + no-arg-constructor default + include in `UserResponse`/`UserInfo` records
- [x] `UserRepository.java` — no changes needed (role on entity is enough)
- [x] `TrackController.java` — in `/api/tracks/upload`, resolve user then check `user.getRole() != GUEST`, else return 403
- [x] `UserAdminController.java` — `GET /api/admin/users` + `PATCH /api/admin/users/{id}/role` — caller must be ADMIN, 403 if not, 404 if target missing
- [x] `SecurityConfig.java` — no Spring Security role wiring needed; kept as-is
- [x] `AuthStatusController.java` — include `role` in `UserInfo` record
- [x] `UserController.java` — include `role` in `UserResponse`

## Tests

- [x] `TrackUploadIT.java` — guest user gets 403 on upload; `@BeforeEach` sets `role = USER` for existing tests
- [x] `UserAdminControllerIT.java` — 5 tests: list as guest (403), list as admin (200), change role as admin, change role as non-admin (403), unknown user (404)
- [x] `AuthStatusControllerIT.java` — asserts `role` field in status response

## Frontend

- [x] `App.tsx` — pass `role` from auth status to `<Profile>`
- [x] `Profile.tsx` — hide upload panel for GUEST, show "pending approval" message instead

## Definition of Done

- [x] `./mvnw spotless:check` green
- [x] `./mvnw test` green
- [x] `./mvnw -Pintegration-test verify` green (30/30)
