# Task List — 2026-06-03_15-31 — v0.0.26

> Frontend: replace track list `<ul>` with a table showing key columns, add a detail view for all fields on one track, add server-side paging prepared for large FIT libraries.
> Confirmed: page size 50, detail inline below table, no speed column.

## Backend — paging endpoint

- [x] Add `Page<Track> findByUser(User user, Pageable pageable)` to `TrackRepository`
- [x] Add `TrackPage` record to `TrackController`: `{ List<TrackSummary> content, long totalElements, int totalPages, int number, int size }`
- [x] Change `GET /api/tracks` to accept `page` (default 0) and `size` (default 50) query params; use `PageRequest.of(page, size, Sort.by(DESC, "startTime").and(Sort.by(DESC, "uploadTimestamp")))` to sort recent first, nulls last
- [x] Return `TrackPage` instead of `List<TrackSummary>`

## Backend — tests

- [x] Update `TrackControllerIT.noTracks_returnsEmptyPage`: deserialise as `TrackPage`, assert `content` empty and `totalElements == 0`
- [x] Update `TrackControllerIT.withTracks_returnsSummariesInPage`: assert against `content` list in `TrackPage`
- [x] Update `TrackControllerIT.sourceId_nullWhenNotSet`: same
- [x] Add `TrackControllerIT.paging_returnsCorrectPage`: save 25 tracks, fetch `?page=1&size=10`, assert 10 items and `totalElements == 25`

## Frontend — types & data fetching

- [x] Extend `Track` type in `TrackList.tsx` to include all FIT fields
- [x] Add `TrackPage` response type
- [x] Update fetch to `GET /api/tracks?page=${page}&size=50`
- [x] Track `page` state and `trackPage` state

## Frontend — table

- [x] Replace `<ul>` with `<table>` — columns: Date, Name, Sport, Distance, Duration, Ascent, Avg HR, Calories
- [x] Date = `startTime` formatted as locale date; fallback to `uploadTimestamp`
- [x] Distance, Duration, Ascent, HR via `format.ts` helpers

## Frontend — detail view

- [x] Add `TrackDetail.tsx` — all fields in labelled table rows, skips nulls
- [x] Click row → show detail inline below table; click again → dismiss

## Frontend — paging controls

- [x] "Page X of Y · N tracks" label
- [x] Prev / Next buttons disabled at boundaries
- [x] Page change scrolls to top of list

## Definition of Done

- [x] `./mvnw spotless:check` green
- [x] `./mvnw verify` green (45 tests)
- [x] `npm run build` green (no type errors)
