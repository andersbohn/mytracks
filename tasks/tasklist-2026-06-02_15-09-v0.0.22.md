# FIT File Upload — Garmin Activity Upsert
> PUT /api/tracks/fit/{activityId} — parse FIT via com.garmin:fit, upsert/merge on sourceId, expand Track entity with FIT metadata subset.

## Dependency
- [x] Add `com.garmin:fit:21.176.0` to `pom.xml`

## DB Migration (V5)
- [x] `V5__add_fit_fields_to_tracks.sql` — add nullable columns to `tracks`:
  - `start_time TIMESTAMPTZ`
  - `duration_seconds INTEGER`
  - `moving_time_seconds INTEGER`
  - `distance_meters NUMERIC(10,2)`
  - `ascent_meters NUMERIC(8,2)`
  - `descent_meters NUMERIC(8,2)`
  - `avg_heart_rate INTEGER`
  - `max_heart_rate INTEGER`
  - `avg_speed_ms NUMERIC(7,4)`
  - `max_speed_ms NUMERIC(7,4)`
  - `calories INTEGER`
  - `avg_power_watts INTEGER`
  - `normalized_power_watts INTEGER`
  - `avg_cadence INTEGER`
  - `sport VARCHAR(64)`
  - `sub_sport VARCHAR(64)`
- [x] Unique partial index: `CREATE UNIQUE INDEX uq_tracks_user_source ON tracks(user_id, source_id) WHERE source_id IS NOT NULL`

## Domain
- [x] `FitMetadata.java` record — all fields above
- [x] `FitParser.java` — uses SDK `Decode` + `MesgBroadcaster` + `SessionMesgListener` to extract `FitMetadata` from `InputStream`
- [x] `Track.java` — add setters/fields for all new columns; keep existing fields unchanged

## Repository
- [x] `TrackRepository` — add `findByUserAndSourceId(User user, String sourceId): Optional<Track>`

## Controller
- [x] `FitUploadController.java` — `PUT /api/tracks/fit/{activityId}` (multipart `file`)
  - GUEST → 403
  - Find or create `Track` by `(user, activityId)`
  - Parse FIT from uploaded bytes → `FitMetadata`
  - Merge all metadata fields onto track (upsert semantics)
  - Set `rawPayload` to uploaded bytes
  - Save and return `TrackSummary`

## API response
- [x] `TrackSummary` — extend with FIT fields (start_time, distance_meters, duration_seconds, sport, heart rate, etc.)
- [x] `TrackController.GET /api/tracks` — new fields appear in list response (same `from()` factory)

## Frontend
- [x] `UploadPanel.tsx` — separate FIT upload section (file input accepting `.fit`, calls `PUT /api/tracks/fit/{activityId}` with activityId input field)

## Tests
- [x] `FitParserTest.java` — unit tests with synthetic `.fit` built via `BufferEncoder`; assert key fields parsed correctly
- [x] `FitUploadControllerIT.java` — integration tests:
  - Upload FIT as USER → 200, fields persisted
  - Second upload same activityId → 200, updated (upsert)
  - Upload as GUEST → 403
  - Missing file → 400
- [x] Existing tests still pass (35/35 green)

## Definition of Done
- [x] `./mvnw spotless:check` green
- [x] `./mvnw test` green
- [x] `./mvnw verify` green (35 tests)
- [x] All checklist items above checked off
