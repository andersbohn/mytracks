# Task List — 2026-06-03_13-43 — v0.0.25

> VPS boot failure: V5 migration used `NUMERIC` for float columns but JPA entity maps `Double` to `float8`/DOUBLE PRECISION. Hibernate `validate` mode rejects the mismatch. Fix schema + add IT that catches this before deploy.
>
> Error: `Schema-validation: wrong column type encountered in column [ascent_meters] in table [tracks]; found [numeric (Types#NUMERIC)], but expecting [float(53) (Types#FLOAT)]`
>
> Affected columns (all `Double` in entity, all `NUMERIC` in V5): `distance_meters`, `ascent_meters`, `descent_meters`, `avg_speed_ms`, `max_speed_ms`.

## Schema fix

- [x] Add `V6__fix_double_precision_columns.sql`: `ALTER TABLE tracks ALTER COLUMN` for all 5 affected columns → `TYPE DOUBLE PRECISION`

## Schema validation IT

- [x] Add `SchemaValidationIT.java` with `@SpringBootTest` + `@Testcontainers` + PostgreSQL container + `@TestPropertySource(properties = "spring.jpa.hibernate.ddl-auto=validate")` — single `contextLoads()` test; fails if any entity/column mismatch exists

## Definition of Done

- [x] `./mvnw spotless:check` green
- [x] `./mvnw test` green
- [x] `./mvnw verify` green (44 tests, SchemaValidationIT passing)
