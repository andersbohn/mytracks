# OpenTelemetry — 2026-05-29_19-05 — v0.0.14

> Request: Add standard OpenTelemetry to backend (Spring Boot) and frontend (React). Both point at the existing Jaeger instance — docker-compose locally, k8s jaeger-collector in prod.

---

## Backend

- [x] `pom.xml` — add `micrometer-tracing-bridge-otel` + `opentelemetry-exporter-otlp`
- [x] `application.properties` — `management.tracing.sampling.probability=1.0` + `management.otlp.tracing.endpoint=${MANAGEMENT_OTLP_TRACING_ENDPOINT:}` (blank = no-op when unset)
- [x] `application-local.properties` — `management.otlp.tracing.endpoint=http://localhost:4318/v1/traces`
- [x] `src/test/resources/application.properties` — `management.tracing.enabled=false`
- [x] `app-deployment.yaml` — `MANAGEMENT_OTLP_TRACING_ENDPOINT=http://jaeger-collector.default.svc.cluster.local:4318/v1/traces`

## Frontend

- [x] `npm install` — `@opentelemetry/sdk-trace-web` `@opentelemetry/exporter-trace-otlp-http` `@opentelemetry/instrumentation-fetch` `@opentelemetry/instrumentation-document-load` `@opentelemetry/resources`
- [x] `src/telemetry.ts` — `WebTracerProvider` + `FetchInstrumentation` + `DocumentLoadInstrumentation` + `OTLPTraceExporter` → `/otlp/v1/traces`
- [x] `src/main.tsx` — `initTelemetry()` before `ReactDOM.createRoot`
- [x] `vite.config.ts` — dev proxy `/otlp` → `http://localhost:4318`

## Infrastructure

- [x] `traefik-ingress.yaml` — `/otlp` path → `jaeger-otlp` service (port 4318)
- [x] ExternalName Service `jaeger-otlp` in `mytracks` namespace → `jaeger-collector.default.svc.cluster.local`

## Tests

- [x] All existing ITs green with `management.tracing.enabled=false` in test properties (31/31)
- [x] `TracingConfigIT` — app starts and responds normally when `management.tracing.enabled=true` (no OTLP endpoint set → no-op exporter)

## Definition of Done

- [x] `./mvnw spotless:check` green
- [x] `./mvnw test` green
- [x] `./mvnw -Pintegration-test verify` green (31/31)
- [x] Task list checked off
